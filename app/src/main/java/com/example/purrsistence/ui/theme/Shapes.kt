package com.example.purrsistence.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

object Shapes {
    val inputs = RoundedCornerShape(8.dp)   // inputs
    val buttons = RoundedCornerShape(12.dp) // buttons
    val cards = RoundedCornerShape(16.dp)  // cards
}