package com.example.purrsistence.ui.theme

import androidx.compose.ui.unit.dp

// Example usage: modifier = Modifier.padding(Spacing.lg)

object Spacing {
    val xxs = 4.dp // very tight (only inside components)
    val xs = 6.dp // inside components
    val sm = 8.dp // inside components
    val md = 12.dp // medium default gaps (often between items)
    val lg = 16.dp // default screen padding / margins
    val xl = 24.dp // between sections
    val xxl = 32.dp // major separation
}